# Development Environment Variables
NODE_ENV=development
VITE_USE_DYNAMODB=false

# For development, we use localStorage instead of DynamoDB
# Set to true only if you have AWS credentials configured and want to test DynamoDB locally
# VITE_USE_DYNAMODB=true
# AWS_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
