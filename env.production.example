# Production Environment Variables
NODE_ENV=production
VITE_USE_DYNAMODB=true

# AWS Configuration
AWS_REGION=eu-north-1
TENANT_TABLE_NAME=fsm-appointment-tenants

# API Gateway Configuration
VITE_TENANT_API_URL=https://40be8c42uc.execute-api.eu-north-1.amazonaws.com/dev

# SAP FSM Configuration (will be set by deployment)
VITE_SAP_CLIENT_ID=your-production-client-id
VITE_SAP_CLIENT_SECRET=your-production-client-secret
VITE_SAP_TOKEN_URL=https://your-production-token-url

# Optional: AWS Credentials (if not using IAM roles)
# AWS_ACCESS_KEY_ID=your-access-key
# AWS_SECRET_ACCESS_KEY=your-secret-key
